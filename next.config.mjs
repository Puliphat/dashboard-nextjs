/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'fastly.picsum.photos', 'static.bandainamcoent.eu', 'plus.unsplash.com'],
    },
}
export default nextConfig;
